# Agents

This package has no dependencies. Do not add any.

Write vanilla JavaScript only. No TypeScript, no compiler, and no bundler.

Export functions. Do not export a class or a default object as the public API.

Do not use Node.js-only APIs. The package is published to npm so any runtime can import it.
